<div class="container">
  <div>
    <div class="row">
      <div><%= image_tag @user.get_profile_image(100,100), class:"rounded-circle"; %></div>
      <div>
        <h4><%= @user.account_name %></h4>
        <% if user_signed_in? %>
          <% if @user.id == current_user.id %>
            <%= link_to "プロフィール編集", edit_users_path(current_user) %>
          <% else %>
            フォロー
          <% end %>
        <% end %>
      </div>
    </div>
    <div>

    </div>
  </div>
</div>
Footer
