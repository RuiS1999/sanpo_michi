<div class="container">
  <div>
    <div class="row">
      <h2>Results index</h2>
      <%= render "layouts/search_user"%>
    </div>
    <div>
      <table class="table text-center">
        <thead>
          <tr>
            <th>ユーザー名</th>
            <th>投稿数</th>
            <th>フォロワー数</th>
            <th></th>
          </tr>
        </thead>
        <tbody>
          <% @users.each do |user| %>
            <tr>
              <td>
                <%= link_to user_path(user.user_name), class: "text-decoration-none" do %>
                  <%= user.account_name %>
                <% end %>
              </td>
              <td><%= user.posts.count %></td>
              <td><p class="text-small"><%= link_to user.followings.count, followings_user_path(user) %> フォロー <%= link_to user.followers.count, followers_user_path(user) %> フォロワー</p></td>
              <td>
                <% if user_signed_in? %>
                  <% unless user == current_user %>
                    <% if user.is_followed_by?(current_user) %>
                      <%= link_to user_relationships_path(user.id), method: :delete do %>
                        <button type="button" class="btn btn btn-danger">フォロー解除</button>
                      <% end %>
                    <% else %>
                      <%= link_to user_relationships_path(user.id), method: :post do %>
                        <button type="button" class="btn btn btn-primary">フォロー</button>
                      <% end %>
                    <% end %>
                  <% end %>
                <% else %>
                  <%= link_to user_relationships_path(user.id), method: :post do %>
                    <button type="button" class="btn btn btn-primary">フォロー</button>
                  <% end %>
                <% end %>
              </td>
            </tr>
          <% end %>
        </tbody>
      </table>
    </div>
  </div>
</div>